package com.tutorspoint.search.repository;

/** The filters that get per-value counts. Each is a many-valued attribute of a profile. */
public enum FacetDimension {
    SUBJECT,
    EXAM_LEVEL,
    SYLLABUS,
    MEDIUM,
    CLASS_FORMAT,
    AREA
}
